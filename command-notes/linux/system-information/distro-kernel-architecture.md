# Distro / Kernel / Architecture

## Linux

<div class="line-title bold-text">Quick multi-info when available:</div>

```bash
hostnamectl 2>/dev/null
```

<div class="line-title bold-text">Alternatives / fallback:</div>

```bash
hostname
```

```bash
cat /etc/hostname 2>/dev/null
```

```bash
cat /etc/os-release 2>/dev/null
```

```bash
uname -a
```

```bash
arch
```

```bash
id
```
