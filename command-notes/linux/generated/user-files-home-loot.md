# User Files / Home Loot — {{entity}}

## Recommended → Fallback

```bash
tree -a -L 4 {{home}}
```

```bash
find {{home}} -maxdepth 4 -print 2>/dev/null
```

```bash
ls -laR {{home}} 2>/dev/null
```
