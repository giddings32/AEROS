# Home Loot File — {{path}}

Collected for **{{user}}**.

```bash
ls -la {{quoted_path}} 2>/dev/null
```

```bash
file {{quoted_path}} 2>/dev/null
```

```bash
cat {{quoted_path}} 2>/dev/null
```

```bash
sed -n '1,200p' {{quoted_path}} 2>/dev/null
```

Use the associated AEROS output box to store file contents or findings. Backtick important values so they glow in preview.
